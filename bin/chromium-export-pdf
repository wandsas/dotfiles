#!/bin/sh

die () {
    local ret="$?"
    echo "$@"
    exit "$ret"
}

command -v chromium &>/dev/null || die "chromium binary not found, aborted"

[[ "$#" != 1 ]] && die "Usage: ${0##*/} <url/of/pdf/to/export>"

chromium --headless --disable-gpu --print-to-pdf=chromium.pdf "$1"
